思考问题1
为什么：
auto p2 = move(p1);
之后：
p1 == nullptr
？
你的回答：
因为这是移动赋值，p1会将资源控制权转交给p2，而自己不再控制资源，故将其置空。

思考问题2
为什么make_shared比：
shared_ptr<T>(new T)
效率更高？
你的回答：因为make_shared一次分配一块大内存，而不是像后者一样分配两次内存，减少了malloc次数、内存碎片、分配开销故效率更高。

思考问题3
为什么weak_ptr访问对象需要：
lock()
而不能直接：
w->xxx
？
你的回答：
因为weak_ptr只是观察对象，不拥有对象。